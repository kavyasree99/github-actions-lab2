name: Installing Python

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Set up Python
      uses: actions/setup-python@v5 #reusable workflow
      with:
        python-version: 3.10
        

